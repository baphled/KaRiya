// Package shared provides reusable view implementations that are not
// domain-specific. Views handle only the content section inside an intent;
// the intent owns chrome (breadcrumbs, logo, footer) via ScreenLayout.
//
//nolint:revive,nolintlint // "shared" is intentional — cross-domain reusable views
package shared
