// Package configure provides view implementations for the configure settings domain.
// Views handle only the content section inside an intent; the intent owns
// chrome (breadcrumbs, logo, footer) via ScreenLayout.
package configure
